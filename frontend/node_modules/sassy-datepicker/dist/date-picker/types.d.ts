export declare type WeekStartDay = 'Sunday' | 'Monday';
export declare type DatePickerOptions = {
    /**
     * Week starts from which day
     */
    weekStartsFrom: 'Sunday' | 'Monday';
};
export declare type DisplayDate = {
    date: Date;
    active: boolean;
    ms: number;
};
